# syntax=localhost:5000/dofigen:frontend-local

context:
  - logo.svg
fromImage: alpine
workdir: /app
env: 
  RUST_LOG: debug
copy: logo.svg
run: echo Hello World!
volume:
  - /data
expose:
  - 80
  - port: 12345
    protocol: udp
label:
  maintainer: taorepoara
entrypoint: "/bin/sh"
cmd: ["-c", "cat logo.svg"]