{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "stage",
    "type": "object",
    "properties": {
        "image": {
            "type": "string",
            "description": "The docker image on which the stage is base on",
            "default": "scratch"
        }
    }
}